Hallo <%= @user.username %>,

du hast angefordert dein Passwort zurückzusetzen. Klicke auf den untenstehenden Link, um ein neues Passwort zu vergeben:

<%= edit_password_url(@token) %>

Dieser Link ist gültig für <%= distance_of_time_in_words(0, @user.password_reset_token_expires_in) %>.

Wenn du das nicht angefordert hast, kannst du diese Email ignorieren.

Prost,
Dein CocktailScout Team
