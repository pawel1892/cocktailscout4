<div class="max-w-md mx-auto mt-10 p-6 bg-white rounded shadow-md border border-gray-200">
  <h1 class="text-2xl font-bold mb-6 text-center text-cs-dark-red">Bestätigung erneut senden</h1>

  <%= tag.div(flash[:alert], class: "mb-4 text-red-600 bg-red-50 p-3 rounded") if flash[:alert] %>

  <%= form_with url: confirmations_path do |form| %>
    <div class="form-group">
      <%= form.label :email_address, "E-Mail-Adresse", class: "label-field" %>
      <%= form.email_field :email_address, required: true, autocomplete: "email", placeholder: "name@example.com", class: "input-field" %>
    </div>

    <div class="flex items-center justify-between">
      <%= form.submit "Anleitung senden", class: "btn btn-primary w-full" %>
    </div>
  <% end %>

  <div class="mt-4 text-center text-sm">
    Bereits bestätigt? <%= link_to "Login", new_session_path, class: "text-cs-dark-red hover:underline" %>
  </div>
</div>
