# ds
- **ds** (`.claude/skills/ds/SKILL.md`) - CocktailScout design system: Tailwind-first, cs-* color tokens only, DRY component reuse. Trigger: `/ds`
When the user types `/ds`, invoke the Skill tool with `skill: "ds"` before doing anything else.
